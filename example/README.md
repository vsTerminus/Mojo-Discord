# DEPENDENCIES

using cpanm, `cpanm --installdeps .`

This demo module required FindBin >= 1.51

# Configuring the Bot

Make sure your bot has at least "Send Messages", "Read Message History", and "Add Reactions" when you invite it to your server.

